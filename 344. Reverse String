class Solution {
public:
    void reverse(vector<char>& s, int n){
    int start = 0;
    int end = n-1;

    while(start<end){
        swap(s[start++], s[end--]);
    }
} 
    
    void reverseString(vector<char>& s) {
         int n = s.size();
         reverse(s, n);      
    }
};
